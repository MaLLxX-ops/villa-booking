"use client";

import { useEffect, useState } from "react";

type Villa = { id: string; name: string }; type Entry = { villa_id: string; date: string; is_available: boolean; note: string | null };
export default function AdminAvailabilityClient() { const [villas, setVillas] = useState<Villa[]>([]); const [villaId, setVillaId] = useState(""); const [date, setDate] = useState(""); const [entries, setEntries] = useState<Entry[]>([]); const [error, setError] = useState(""); useEffect(() => { fetch("/api/admin/villas").then((r) => r.json()).then((data) => { setVillas(data); if (data[0]) setVillaId(data[0].id); }); }, []); const load = () => villaId && fetch(`/api/admin/availability?villa_id=${villaId}`).then((r) => r.json()).then(setEntries).catch(() => setError("Gagal memuat kalender.")); useEffect(() => { load(); }, [villaId]); const mark = async (is_available: boolean) => { if (!date || !villaId) return; const response = await fetch("/api/admin/availability", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ villa_id: villaId, date, is_available, note: is_available ? "Tersedia" : "Penuh" }) }); if (!response.ok) setError("Gagal menyimpan tanggal."); else { setDate(""); load(); } }; const remove = async (entry: Entry) => { await fetch("/api/admin/availability", { method: "DELETE", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ villa_id: entry.villa_id, date: entry.date }) }); load(); }; return <section><h1 className="text-3xl font-black text-navy">Availability</h1><p className="mt-2 text-stone">Tandai tanggal yang penuh atau buka kembali tanggal tertentu.</p><div className="mt-7 bg-white border border-sand rounded-2xl p-5 flex flex-col sm:flex-row gap-3"><select value={villaId} onChange={(e) => setVillaId(e.target.value)} className="field"><option value="">Pilih villa</option>{villas.map((villa) => <option key={villa.id} value={villa.id}>{villa.name}</option>)}</select><input type="date" value={date} onChange={(e) => setDate(e.target.value)} className="field" /><button onClick={() => mark(false)} className="rounded-lg bg-red-600 text-white px-4 py-3 font-black">Tandai Penuh</button><button onClick={() => mark(true)} className="rounded-lg bg-sage-dark text-white px-4 py-3 font-black">Tersedia</button></div>{error && <p className="mt-4 text-red-600 font-bold">{error}</p>}<div className="mt-6 bg-white border border-sand rounded-2xl divide-y divide-sand">{entries.map((entry) => <div key={entry.date} className="p-4 flex justify-between items-center"><span className="font-bold text-navy">{entry.date} <span className={entry.is_available ? "text-sage-dark" : "text-red-600"}>{entry.is_available ? "Tersedia" : "Penuh"}</span></span><button onClick={() => remove(entry)} className="text-xs font-bold text-stone hover:text-red-600">Hapus</button></div>)}{entries.length === 0 && <p className="p-5 text-stone">Belum ada penanda tanggal.</p>}</div></section>; }