"use client";

import { FormEvent, useEffect, useState } from "react";
import { Plus, Save, Trash2 } from "lucide-react";

type Villa = { id: string; name: string; slug: string; price: number; bedrooms: number; bathrooms: number; max_guests: number; owner_whatsapp: string; is_active: boolean; description: Record<string, string>; location_area: Record<string, string>; images: string[]; category: Record<string, string>; category_key: string; amenities: Record<string, string[]>; latitude: number; longitude: number; };
const empty = { name: "", slug: "", price: "", bedrooms: "1", bathrooms: "1", max_guests: "2", owner_whatsapp: "", location: "", description: "", images: "" };

export default function AdminVillasClient() {
  const [villas, setVillas] = useState<Villa[]>([]); const [form, setForm] = useState(empty); const [message, setMessage] = useState("");
  const load = () => fetch("/api/admin/villas").then((response) => response.json()).then(setVillas).catch(() => setMessage("Gagal memuat villa."));
  useEffect(() => { load(); }, []);
  const add = async (event: FormEvent) => { event.preventDefault(); const locale = { id: form.location, en: form.location, fr: form.location, zh: form.location, ja: form.location, ko: form.location }; const description = { id: form.description, en: form.description, fr: form.description, zh: form.description, ja: form.description, ko: form.description }; const { name, slug, price, bedrooms, bathrooms, max_guests, owner_whatsapp, images } = form; const response = await fetch("/api/admin/villas", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id: slug, name, slug, price: Number(price), bedrooms: Number(bedrooms), bathrooms: Number(bathrooms), max_guests: Number(max_guests), owner_whatsapp, location_area: locale, description, images: images.split(",").map((item) => item.trim()).filter(Boolean), category: { id: "Villa", en: "Villa", fr: "Villa", zh: "Villa", ja: "Villa", ko: "Villa" }, category_key: "family", amenities: { id: [], en: [], fr: [], zh: [], ja: [], ko: [] }, latitude: -8.65, longitude: 115.2, is_active: true, is_trending: false }) }); if (!response.ok) { setMessage((await response.json()).error || "Gagal menambah villa."); return; } setForm(empty); setMessage("Villa berhasil ditambahkan."); load(); };
  const update = async (villa: Villa) => { const response = await fetch("/api/admin/villas", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id: villa.id, price: villa.price, is_active: villa.is_active }) }); setMessage(response.ok ? "Villa diperbarui." : "Gagal memperbarui villa."); };
  const remove = async (id: string) => { if (!window.confirm("Hapus villa ini?")) return; const response = await fetch("/api/admin/villas", { method: "DELETE", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id }) }); if (response.ok) load(); else setMessage("Gagal menghapus villa."); };
  return <section><h1 className="text-3xl font-black text-navy">Villas</h1><p className="mt-2 text-stone">Kelola katalog, harga, foto, dan status publikasi.</p>{message && <p className="mt-4 text-sm font-bold text-terracotta-dark">{message}</p>}<form onSubmit={add} className="mt-7 bg-white border border-sand rounded-2xl p-5 grid sm:grid-cols-2 lg:grid-cols-4 gap-3"><input required placeholder="Nama villa" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} className="field" /><input required placeholder="ID/slug unik" value={form.slug} onChange={(e) => setForm({ ...form, slug: e.target.value })} className="field" /><input required type="number" placeholder="Harga" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} className="field" /><input required placeholder="WhatsApp pemilik" value={form.owner_whatsapp} onChange={(e) => setForm({ ...form, owner_whatsapp: e.target.value })} className="field" /><input placeholder="Lokasi" value={form.location} onChange={(e) => setForm({ ...form, location: e.target.value })} className="field" /><input placeholder="Kamar" type="number" value={form.bedrooms} onChange={(e) => setForm({ ...form, bedrooms: e.target.value })} className="field" /><input placeholder="Kamar mandi" type="number" value={form.bathrooms} onChange={(e) => setForm({ ...form, bathrooms: e.target.value })} className="field" /><input placeholder="Kapasitas" type="number" value={form.max_guests} onChange={(e) => setForm({ ...form, max_guests: e.target.value })} className="field" /><input placeholder="URL foto, pisahkan koma" value={form.images} onChange={(e) => setForm({ ...form, images: e.target.value })} className="field sm:col-span-2" /><input placeholder="Deskripsi" value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} className="field sm:col-span-2" /><button className="inline-flex items-center justify-center gap-2 rounded-lg bg-terracotta text-white py-3 font-black sm:col-span-2 lg:col-span-4"><Plus className="w-4 h-4" />Tambah villa</button></form><div className="mt-7 overflow-x-auto bg-white border border-sand rounded-2xl"><table className="w-full text-left text-sm"><thead><tr className="border-b border-sand text-stone"><th className="p-4">Villa</th><th className="p-4">Harga</th><th className="p-4">Aktif</th><th className="p-4">Aksi</th></tr></thead><tbody>{villas.map((villa) => <tr key={villa.id} className="border-b border-sand last:border-0"><td className="p-4 font-bold text-navy">{villa.name}<span className="block text-xs text-stone">{villa.slug}</span></td><td className="p-4"><input type="number" value={villa.price} onChange={(e) => setVillas(villas.map((item) => item.id === villa.id ? { ...item, price: Number(e.target.value) } : item))} className="field w-36" /></td><td className="p-4"><input type="checkbox" checked={villa.is_active} onChange={(e) => setVillas(villas.map((item) => item.id === villa.id ? { ...item, is_active: e.target.checked } : item))} /></td><td className="p-4 flex gap-2"><button title="Simpan" onClick={() => update(villa)} className="icon-button"><Save className="w-4 h-4" /></button><button title="Hapus" onClick={() => remove(villa.id)} className="icon-button text-red-600"><Trash2 className="w-4 h-4" /></button></td></tr>)}</tbody></table></div></section>;
}